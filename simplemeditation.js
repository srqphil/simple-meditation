//This function will begin meditation session after the user has selected a valid time
function $() {document.getElementById();}

function begin () {
    if ($(time) != "select time") {
        $("welcome").style.display = "none"
    }

    else {
        $(error).innerHTML = "please select a valid time";
    }
}